<div class="hero">
	<div class="hero-inner">
		<div class="hero-copy">
      <h1><%= title %></h1>
      <p><%= text %></p>
		</div>
    <% if button_type == :link %>
      <%= link %>
    <% else %>
      <label for="<%= modal_id %>">
        <div class="button js-btn"><%= modal_text %></div>
      </label>
    <% end %>
	</div>
</div>
